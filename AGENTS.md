# AGENTS.md

이 문서는 **AI agent (LLM, Copilot, Cursor 등)** 를 위한 지침 파일입니다.  
이 프로젝트에서 코드를 생성·수정·추가할 때 **반드시 이 문서를 기준으로 판단**해야 합니다.

사람이 이 프로젝트를 이해하려면 README.md부터 읽으세요.  
AI agent라면 이 파일을 최우선 기준으로 따르세요.

---

## 1. 기본 원칙 (Core Principles)

### 1.1 언어 (Language)

- 모든 설명, 주석, 문서, 제안은 **항상 한글**을 사용합니다.
- 코드 자체는 영어를 유지하되, **의도·맥락·판단 근거는 반드시 한글로 작성**합니다.
- 영어 주석은 작성하지 않습니다.

### 1.2 AI Agent의 태도

- **실행자가 아닌 조력자**: 단순히 시키는 대로 코드를 바꾸는 것이 아니라, "왜 바꾸는지", "이게 최선인지"를 판단하고 제안해야 합니다.
- **리팩토링 주의**: 사용자가 명시적으로 요청하지 않은 구조 변경, 파일 통합, 공통화는 수행하지 않습니다. 대신 개선이 필요하다면 먼저 **제안**합니다.

---

## 2. 주석 작성 규칙: "계약(Contract)을 남겨라"

이 프로젝트의 주석은 “현재 구현을 정당화”하기 위한 것이 아니라, **이 코드가 반드시 만족해야 하는 목적(의도/계약)** 을 남겨 추후 개선을 돕기 위한 장치입니다.

### 2.1 주석 작성의 배경 (The "Why")

구현이 기술적 한계로 인해 이상적이지 않더라도, **원래 목표**가 무엇인지 기록되어야 합니다.

목표가 **[A(버튼 클릭) → B(준비 메시지 수신) → C(데이터 전송)]** 인 흐름을 가정해 봅시다.
하지만 B 메시지 감지가 기술적으로 어려워, 코드는 **[A(버튼 클릭) → setTimeout(지연) → C(데이터 전송)]** 으로 구현될 수 있습니다.

이때 **의도/목적(Contract)**이 적혀있다면:

> "목적: **[A(버튼 클릭) → B(준비 메시지 수신) → C(데이터 전송)]**"
> → 결과: AI나 후임자는 타임아웃이 **임시 방편(Workaround)**임을 이해하고, 추후 B 메시지를 감지하는 이벤트 리스너로 **올바르게 개선**합니다.

#### 의도가 코드에서 드러나지 않는 선택의 예: `key` 변경을 통한 전체 교체

React에서 컴포넌트의 일부 상태나 로직을 수정하는 대신, `key` 값을 변경하여 **컴포넌트 전체를 언마운트 후 재마운트**하는 선택을 하는 경우가 있습니다.

이 방식은 의도적으로 상태를 초기화하거나, 라이프사이클을 처음부터 다시 실행하기 위한 **정당한 설계 선택**일 수 있습니다.

그러나 이 선택은 코드만 보았을 때:

- 성능 최적화인지
- 버그 회피를 위한 우회인지
- 단순한 실수인지
- 혹은 반드시 유지되어야 할 핵심 계약인지

를 **판단할 수 없습니다.**

따라서 `key` 변경을 통해 컴포넌트 전체를 교체하는 경우, 그 이유와 보장해야 할 동작은 **반드시 주석(Contract)으로 명시**되어야 합니다.

주석이 없다면, 해당 코드는 추후 리팩토링 과정에서 “불필요한 코드” 또는 “이상한 구현”으로 오해받아 제거될 수 있으며, 이는 의도된 동작 변경으로 이어질 수 있습니다.

### 2.2 실전 예시 (React Key Force Remount)

```tsx
/**
 * 일별 방문자 수 데이터를 시각화하기 위한 막대 차트 컴포넌트
 *
 * - 외부에서 전달받은 data를 차트 표현에 적합한 형태로 가공하여
 * BarChart 컴포넌트에 전달한다.
 */
function DailyVisitBarChart({ data }: { data: DailyVisitData[] }) {
  ...

  // 데이터가 변경되면, 해당 변경 사항이 동일한 차트 인스턴스에 즉시 반영되어야 한다.
  return <BarChart key={data.length} data={data} />
}
```

위 코드를 보면,
*데이터 변경 시 변경 사항을 차트에 반영하기 위해 `key` 값을 변경*하는 선택을 했다는 것을 추측할 수 있습니다.

그러나 이 선택은 _코드만 보았을 때_:

- 데이터 갱신을 위한 의도된 설계인지
- 차트가 정상적으로 갱신되지 않는 문제를 우회하기 위한 임시 해결책인지
- 혹은 차트 인스턴스를 매번 새로 생성해야 하는 계약이 있는지

를 판단할 수 없습니다.

이처럼 key 변경을 통해 컴포넌트 전체를 재마운트하는 방식은 의도가 코드에서 자연스럽게 드러나지 않기 때문에, 반드시 주석을 통해 “무엇을 보장해야 하는지(Contract)”를 명확히 남겨야 합니다.

주석이 없다면, 해당 key 변경은 추후 리팩토링 과정에서 불필요한 코드로 오해받아 제거되거나, 의도와 다른 방식으로 수정될 위험이 있습니다.

### 2.3 작성 원칙 요약

- **"어떻게"보다 "무엇을 보장해야 하는지"**: 구현 디테일보다 함수의 책임을 설명합니다.
- **모든 주요 로직에 작성**: 함수, Hook, 비즈니스 로직에는 주석을 남깁니다.

---

## 3. 아키텍처와 구조 (Architecture Rules)

### 3.1 프론트엔드 비소유 코드의 격리

백엔드 API 구조, 외부 스펙, 자동 생성 코드 등 **프론트엔드가 구조를 결정할 수 없는 코드**는 프론트엔드 feature 구조로 가져오지 않습니다.

- **위치**: `src` 디렉토리 최상단 (예: `src/_queries`, `src/api`)
- **규칙**: 구조가 어색해도 백엔드의 구조(계약)를 그대로 따르며, 재배치하거나 리네이밍하지 않습니다.
- **이유**: 구조 일관성보다 **계약 일관성**이 중요하기 때문입니다.

### 3.2 컴포넌트 분리 기준

하나의 컴포넌트 내부에서 `props`에 따라 거대한 `if/else` 분기를 만드는 것을 지양합니다.

- **지양**: `isAdmin`, `isGuest` 같은 역할(Role) props로 UI를 분기하는 만능 컴포넌트.
- **지향**: `AdminHeader`, `UserHeader` 처럼 역할별로 명확히 분리된 컴포넌트.
- **판단 기준**: Boolean props가 2개 이상이거나, props 이름이 "옵션"이 아닌 "권한/역할"을 나타낸다면 컴포넌트를 분리합니다.

### 3.3 단일 파일 책임 원칙

서로 다른 목적을 가진 함수들을 하나의 파일에 몰아넣는 것을 금지합니다.
**하나의 파일이 하나의 논리적 도메인/책임만 갖도록 분리**하는 것을 원칙으로 합니다.

- **Bad Practice**: `auth-actions.ts`라는 파일 하나에 `login`, `logout`, `verify`, `nonce` 등 모든 인증 관련 액션을 몰아넣는 것.
- **Good Practice**: 역할별로 파일을 분리하여, 파일명만 보고도 어떤 액션인지 알 수 있게 하는 것.
  - `generate-nonce-action.ts`
  - `verify-signature-action.ts`
  - `get-session-action.ts`
  - `logout-action.ts`

> **이유**: 하나의 파일에 여러 기능이 섞여 있으면, 코드의 의존성이 복잡해지고 변경 시 영향 범위를 파악하기 어렵습니다. 파일을 나누는 비용을 아끼지 마세요.

### 3.4 Index 파일 생성 금지

폴더의 진입점으로 `index.ts` 또는 `index.tsx`를 생성하는 행위를 금지합니다.
모든 import는 **구체적인 파일명**을 명시해야 합니다.

- **Bad Practice**: `import { UserProfile } from "@/features/user"` (암시적 index 참조)
- **Good Practice**: `import { UserProfile } from "@/features/user/user-profile"` (명시적 파일 참조)

> **이유**:
>
> 1. **순환 참조(Circular Dependency)**: Barrel file(index.ts)은 순환 참조의 주된 원인입니다.
> 2. **검색 및 탐색 어려움**: 프로젝트에 `index.ts`가 수십 개 존재하면 파일명 검색(`Cmd+P`)으로 파일을 찾기 어렵습니다.
> 3. **모호함 제거**: 현대의 IDE는 파일 간 이동이 매우 쉬우므로, 코드를 뭉쳐두는 편의성보다 **명확한 출처**를 우선시합니다.
> 4. **성능 문제**: Index 파일을 통해 import하면, 사용하지 않는 다른 export들까지 불필요하게 로드/분석되어 빌드 성능과 런타임 초기화 속도에 악영향을 줍니다.

### 3.5 Next.js App Router 구조 원칙

`src/app` 디렉토리 내의 파일은 기본적으로 **Server Component**를 유지해야 합니다.

- **Server Component 유지**: `src/app` 내의 `page.tsx`, `layout.tsx` 등은 데이터 페칭과 메타데이터 생성을 담당하는 Server Component여야 합니다. 이 파일들 최상단에 `'use client'`를 선언하는 행위를 **금지**합니다.
- **Client Component 분리**: `useState`, `useEffect`, 이벤트 핸들러 등 클라이언트 로직이 필요한 부분은 반드시 **별도의 파일(Client Component)로 분리**해야 합니다.
  - 분리된 컴포넌트는 `src/features` 또는 `src/components` 등 적절한 위치에 생성한 후, `src/app` 페이지에서 import하여 사용합니다.
- **이유**:
  1. **성능 최적화**: 불필요한 JS 번들 크기를 줄이고, 서버 사이드 렌더링의 이점을 극대화하기 위함입니다.
  2. **책임 분리**: 데이터 로딩(서버)과 상호작용(클라이언트)의 책임을 명확히 분리하기 위함입니다.

---

## 4. 네이밍과 컨벤션 (Naming & Conventions)

### 4.1 파일 및 변수 네이밍

- **형식**: 파일명은 **kebab-case**를 사용합니다.
- **철학**: 축약어 사용을 금지합니다. 이름이 길어져도 **의도가 명확하고 구분 가능한 이름**을 선택합니다.
  - 예: `user.ts` (X) -> `user-profile-data.ts` (O)

### 4.2 컴포넌트 네이밍 패턴 (Suffix 활용)

파일을 열지 않고도 역할을 파악할 수 있도록 접미사를 적극 활용합니다.

| Suffix     | 의미                | 예시                          |
| :--------- | :------------------ | :---------------------------- |
| `-page`    | 라우트 단위 페이지  | `user-profile-page.tsx`       |
| `-section` | 페이지 내 의미 영역 | `pricing-benefit-section.tsx` |
| `-card`    | 반복되는 정보 블록  | `pricing-plan-card.tsx`       |
| `-form`    | 입력/검증 책임 단위 | `signup-form.tsx`             |
| `-modal`   | 독립 레이어 UI      | `delete-account-modal.tsx`    |
| `-dialog`  | 확인/취소 결정 UI   | `confirm-payment-dialog.tsx`  |

### 4.3 아이콘 네이밍 규칙

- **이유**: 이름만으로 **아이콘임이 명확히 구별되어야 하기 때문**입니다. ("이게 컴포넌트인지 아이콘인지" 헷갈리지 않게 합니다.)
- **접미사 필수**: 항상 **`Icon`으로 끝나는 이름**을 사용합니다. (`MenuIcon` ✅ / `Menu` ❌)
- **Lucide 사용 시**: `as` alias를 지양하고 라이브러리가 제공하는 본래 이름(또는 `Icon` 접미사가 포함된 이름)을 그대로 import합니다.
  - `import { ArrowRightIcon } from "lucide-react"` (O)
  - `import { ArrowRight as ArrowRightIcon } ...` (X - 불가피한 경우 제외)

---

## 5. 코딩 표준 (Coding Standards)

### 5.1 CSS / Tailwind (size- 유틸리티)

- **규칙**: 너비와 높이가 같은 경우 `w-4 h-4` 대신 **`size-4`** 유틸리티를 사용합니다.
- **이유**: `w-`와 `h-`를 각각 쓰는 것보다, **"이 요소는 정사각 비율을 가진다"**는 의도를 훨씬 직관적으로 전달하기 때문입니다.

### 5.2 공통 유틸리티 사용

- **로그 (Logging)**: `console.log`, `console.warn`, `console.error` 등 **모든 콘솔 출력 함수**의 직접 사용을 금지합니다. 반드시 프로젝트 내의 **`logger` 클래스(또는 모듈)**를 사용해야 합니다.
  - 이유: 로그 레벨 관리, 운영 환경에서의 노출 제어, 일관된 포맷팅을 위해서입니다.
- **스토리지 (Storage)**: `localStorage`, `sessionStorage`에 직접 접근하는 것을 금지합니다. 반드시 프로젝트 내의 **`storage` 클래스**를 사용해야 합니다.

---

## 요약 (Checklist)

1. [ ] 모든 설명과 주석은 **한글**인가?
2. [ ] 함수에 **목적(Contract)**을 설명하는 주석이 있는가? (단순 구현 설명이 아닌, '보장해야 할 동작' 명시)
3. [ ] 외부 구조(API 등) 코드는 **`src` 최상단**에 그대로 두었는가?
4. [ ] 복잡한 분기 대신 **컴포넌트 분리**를 고려했는가?
5. [ ] **`src/app` 내부는 Server Component**로 유지하고, 클라이언트 로직은 별도 컴포넌트로 분리했는가? (`'use client'` 남용 금지)
6. [ ] 파일명은 **kebab-case**이며, 축약 없이 명확한가?
7. [ ] 아이콘 이름은 **`Icon`**으로 끝나 아이콘임을 명확히 했는가?
8. [ ] Tailwind에서 `size-`를 사용하여 **정사각 비율임**을 명확히 했는가?
9. [ ] 로그(`console.*`)와 스토리지는 **전용 클래스(logger, storage)**를 사용했는가?
